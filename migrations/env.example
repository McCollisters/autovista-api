# Source MongoDB Connection (where data is read from)
MIGRATION_SOURCE_URI=mongodb://localhost:27017/autovista-api-source

# Destination MongoDB Connection (where processed data is written to)
MIGRATION_DEST_URI=mongodb://localhost:27017/autovista-api-destination

# Optional: Override database names
# MIGRATION_SOURCE_DATABASE_NAME=autovista-api-source
# MIGRATION_DESTINATION_DATABASE_NAME=autovista-api-destination

# Optional: Connection timeout in milliseconds
# MIGRATION_TIMEOUT=30000

# Example connection strings for different environments:
# Local MongoDB: mongodb://localhost:27017/database-name
# MongoDB Atlas: mongodb+srv://username:password@cluster.mongodb.net/database-name
# Docker MongoDB: mongodb://mongo:27017/database-name

# Example setup for local development:
# MIGRATION_SOURCE_URI=mongodb://localhost:27017/production-backup
# MIGRATION_DEST_URI=mongodb://localhost:27017/development-db
