# Environment Variables Configuration
# 
# IMPORTANT: This file should NOT contain actual sensitive values (passwords, API keys, etc.)
# Instead, set sensitive values via:
# 1. AWS Console: Elastic Beanstalk → Your Environment → Configuration → Software → Environment properties
# 2. EB CLI: eb setenv MONGODB_DEV_URI="..." SD_USER="..." etc.
#
# This file can be used for non-sensitive defaults or as a reference.
#
# For production, you MUST set these environment variables in AWS:
# - MONGODB_PROD_URI (REQUIRED for production, preferred)
#   OR MONGODB_DEV_URI (REQUIRED fallback - but MONGODB_PROD_URI is recommended)
# - SD_USER (if using Super Dispatch)
# - SD_PASS (if using Super Dispatch)
# - SENDGRID_API_KEY (if sending emails)
# - JWT_SECRET (if using authentication)
# - And other variables as needed (see ENVIRONMENT_VARIABLES.md)

option_settings:
  aws:elasticbeanstalk:application:environment:
    # Server Configuration
    NODE_ENV: "production"
    PORT: "8080"
    
    # AWS Configuration
    AWS_REGION: "us-east-1"
    
    # Database - MUST BE SET VIA AWS CONSOLE OR EB CLI
    # MONGODB_PROD_URI: "mongodb+srv://user:pass@cluster.mongodb.net/dbname" (preferred for production)
    # OR MONGODB_DEV_URI: "mongodb+srv://user:pass@cluster.mongodb.net/dbname" (fallback)
    
    # Super Dispatch - MUST BE SET VIA AWS CONSOLE OR EB CLI
    # SD_USER: "your_client_id"
    # SD_PASS: "your_client_secret"
    
    # SendGrid - MUST BE SET VIA AWS CONSOLE OR EB CLI
    # SENDGRID_API_KEY: "your_sendgrid_api_key"
    
    # JWT - MUST BE SET VIA AWS CONSOLE OR EB CLI
    # JWT_SECRET: "your_jwt_secret"
    
    # Optional: Email Configuration
    # EMAIL_FROM_ADDRESS: "noreply@example.com"
    # EMAIL_FROM_NAME: "Autovista API"
    
    # Optional: SMS Configuration (if using Twilio)
    # TWILIO_ACCOUNT_SID: "your_twilio_account_sid"
    # TWILIO_AUTH_TOKEN: "your_twilio_auth_token"
    # TWILIO_FROM_NUMBER: "+1234567890"
    
    # Optional: Base URL for webhooks
    # BASE_URL: "https://api.yourdomain.com"
    
    # Optional: CORS origins (comma-separated)
    # ALLOWED_ORIGINS: "https://app.yourdomain.com,https://portal.yourdomain.com"
